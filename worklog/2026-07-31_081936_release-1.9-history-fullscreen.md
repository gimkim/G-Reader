# Worklog — 2026-07-31 08:19:36 ICT

## Scope

Release 1.9, recently opened history, and fullscreen overlay navigation.

## Work recorded

- `f07fa43` released 1.9 and added `HistoryPopupForm`, history settings,
  toolbar/hotkey integration, and recent folder/archive/PDF navigation.
- History is newest-first, configurable in retention/count, and opens the item
  in the current reading mode by double-click or Enter.
- Fullscreen top and bottom controls are overlays: the top bar appears near
  the upper viewport percentage and hides immediately after leaving the edge;
  the bottom bar appears near the lower percentage and hides after a delay.
  Neither bar should push or resize the image viewport.
- Settings/performance/codec explanations and the GitHub release update checker
  were treated as user-facing configuration requirements, with confirmation
  before downloading/relaunching a newer version.

## Validation

The 1.9 release was published. Fullscreen pointer-edge behavior and history
navigation remained manual UI validation.
