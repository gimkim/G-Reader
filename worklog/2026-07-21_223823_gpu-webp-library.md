# Worklog — 2026-07-21 22:38:23 ICT

## Scope

GPU rendering, large-library behavior, archive covers, and animated WebP
stability.

## Work recorded

- `8189546` added the GPU interop device, nvJPEG/NPP integration, GPU thumbnail
  and contact-sheet caches, Direct2D upload pacing, color-profile handling,
  and library/folder preview optimizations.
- `659f37d` added GPU double buffering for animated WebP frames.
- Archive preview/caching was bounded and made cancellation-aware to address
  the reported large folder `L:\tgm\(0archive` and black `.webp` thumbnails.
- The intended behavior remained GPU-first; CPU/ImageMagick was retained only
  for unavailable or safely retired native GPU paths.

## Validation

The code and build path were verified. The exact `L:\tgm\(0archive` library
was not claimed as UI-verified in this worklog.
