# Worklog — 2026-08-03 17:37:09 ICT

## Scope

Completed the release handoff requested after the thumbnail/D2D stability
change.

## Build outputs

- Published the normal Windows build to `release` with
  `dotnet publish -c Release -o release --no-restore`.
- Confirmed `release\Fast Reader Viewer.exe` has file version `1.9.1.0`.
- Built the Store upload package with
  `packaging\Store\build-store-msix.ps1`:
  `release\store\FastReaderViewer_1.9.1.0_x64.msix`.
- Created the clean runtime ZIP
  `release\Fast-Reader-Viewer-1.9.1-win-x64.zip` and
  `release\SHA256SUMS-1.9.1.txt`; the ZIP contains only the current runtime,
  native libraries, notices, and license files.

## GitHub handoff

- Pushed commit `32c1b1b` to
  `agent/fix-gpu-thumbnail-stability`.
- Pushed tag `v1.9.1` and published GitHub release **Fast Reader/Viewer 1.9.1**
  with the direct EXE, runtime ZIP, Store MSIX upload artifact, and checksums.
- The Store package is intentionally unsigned locally; Microsoft Store signs
  the package during certification/distribution.

## Validation status

Build and packaging completed successfully. Runtime validation of the reported
large-archive GPU scenario remains a manual test on the target machine/GPU;
this release handoff does not claim that UI test was run by the assistant.
