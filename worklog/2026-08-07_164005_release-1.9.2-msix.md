# Release 1.9.2 MSIX

## Request

Publish the current Fast Reader/Viewer source changes to Git and make the
current release version 1.9.2 with a freshly built Microsoft Store package.

## Changes

- Updated project `Version` to `1.9.2`.
- Updated `AssemblyVersion` and `FileVersion` together to `1.9.2.0`.
- Built a new self-contained x64 Store package from the current working tree.

## Validation

- `dotnet build -c Release --no-restore` passed with 0 warnings and 0 errors.
- `packaging\Store\build-store-msix.ps1` completed successfully after restoring
  the required assets and produced
  `release\store\FastReaderViewer_1.9.2.0_x64.msix`.
- The generated MSIX is 95,128,811 bytes with SHA-256
  `FF73E54384F1937B90823677E23F1717FF6B3A556205B3B3631F8D9FD69466E9`.
- Package inspection confirmed identity `gimkim.FastReaderViewer`, version
  `1.9.2.0`, architecture `x64`, and the packaged executable payload.
- The Release assembly reports file version `1.9.2.0`.

## Remaining manual checks

- The Store package is unsigned locally; Microsoft Store signing and
  certification remain external steps.
- No interactive UI or installation test was run in this session. Build and
  package inspection do not claim a real folder/archive/PDF/GPU regression
  test.
